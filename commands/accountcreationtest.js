const { MessageAttachment } = require("discord.js")

module.exports = {
    name: 'accountcreationtest',
    description: "retarded",
    execute(message, args){
        userinfoget.user.createdAt
    }
}